---
phase: 01-cropping-tool-digital-export
plan: 11
type: execute
wave: 2
depends_on: [01-08, 01-09]
files_modified:
  - src/lib/components/CropEditor.svelte
  - src/lib/components/GuideOverlay.svelte
  - src/lib/components/ExportButtons.svelte
  - src/lib/components/SpecPanel.svelte
  - src/lib/components/PhotoLoader.svelte
  - src/routes/+page.svelte
autonomous: false
gap_closure: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Keyboard hint text below crop frame is clearly legible (not faint)"
    - "Crown hint text is readable against any photo background"
    - "Overall UI feels polished and production-grade, not a prototype"
  artifacts:
    - path: "src/lib/components/CropEditor.svelte"
      provides: "Legible hint text with sufficient contrast"
    - path: "src/lib/components/GuideOverlay.svelte"
      provides: "Crown hint with background pill or multi-shadow for contrast"
  key_links:
    - from: "src/lib/components/CropEditor.svelte"
      to: "User's eyes"
      via: "WCAG AA contrast ratio >= 4.5:1 on hint text"
      pattern: "rgba.*0\\.[5-9]"
---

<objective>
Apply visual polish to the entire UI using the frontend-design plugin, fixing cosmetic gaps (faint hint text, unreadable crown label) and improving overall design quality.

Purpose: The user reported hint text is "very faint", crown/chin labels are "hard to see on white background", and explicitly requested the frontend-design plugin be used for UI polish (requested 3 times, never done). This plan addresses all visual/cosmetic quality issues in a single design pass.

Output: Production-grade visual design across all UI components.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cropping-tool-digital-export/01-08-SUMMARY.md
@.planning/phases/01-cropping-tool-digital-export/01-09-SUMMARY.md

<interfaces>
From src/lib/components/CropEditor.svelte (line 361-364):
```css
/* CURRENT: 28% opacity = ~2.3:1 contrast ratio. WCAG AA needs 4.5:1 */
.kb-hint {
  color: rgba(255, 255, 255, 0.28);
}
```

From src/lib/components/GuideOverlay.svelte (lines 57-75):
```css
/* CURRENT: white text on transparent blue band — invisible on light photos */
.guide-label {
  color: rgba(255, 255, 255, 0.85);
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.8);
}
.crown-hint {
  color: rgba(255, 255, 255, 0.85);
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.8);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI design polish with frontend-design plugin</name>
  <files>src/lib/components/CropEditor.svelte, src/lib/components/GuideOverlay.svelte, src/lib/components/ExportButtons.svelte, src/lib/components/SpecPanel.svelte, src/lib/components/PhotoLoader.svelte, src/routes/+page.svelte</files>
  <action>
    **IMPORTANT: Use the `frontend-design` MCP tool/plugin for this task.** The user explicitly requested this 3 times. The plugin is available as `frontend-design@claude-plugins-official` in the MCP tools.

    Invoke the frontend-design plugin to review and improve the visual design of all UI components. The plugin should analyze the current CSS/markup and produce polished, production-grade styling.

    **Specific cosmetic fixes that MUST be included (from UAT):**

    1. **Hint text contrast (CropEditor.svelte):** Change `.kb-hint` color from `rgba(255, 255, 255, 0.28)` to at least `rgba(255, 255, 255, 0.5)` — target WCAG AA contrast ratio of 4.5:1 against the dark background (#1a1a1e).

    2. **Crown/chin label readability (GuideOverlay.svelte):** The white text with single drop shadow is hard to see on light photo backgrounds. Add a background pill (`background: rgba(0,0,0,0.55); padding: 1px 6px; border-radius: 3px;`) or use a multi-directional text-shadow outline effect (`text-shadow: -1px -1px 0 rgba(0,0,0,0.7), 1px -1px 0 rgba(0,0,0,0.7), -1px 1px 0 rgba(0,0,0,0.7), 1px 1px 0 rgba(0,0,0,0.7);`) to ensure readability against any photo background (light or dark).

    **Broader design pass (use frontend-design plugin judgment):**

    3. Review all component styling for consistency, spacing, typography, and color harmony. The app uses a dark workspace (#1a1a1e) for the crop area and light background for spec panel. Ensure the transition between these feels intentional.

    4. Review button styling (ExportButtons), panel styling (SpecPanel), header (app-header), and landing page for cohesive, modern design.

    5. Review the PhotoLoader component styling.

    **Constraints:**
    - No Tailwind — this project uses scoped Svelte `<style>` blocks with plain CSS
    - No external CSS frameworks or design systems
    - Preserve all functional behavior — this is a CSS/styling-only pass
    - Do not change component structure/hierarchy (that was done in plans 08-09)
    - Dark workspace background (#1a1a1e) is intentional for photo editing context — keep it
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Hint text below crop frame is clearly legible (opacity >= 0.5)
    - Crown/chin labels readable against both light and dark photo backgrounds
    - Overall UI has consistent, polished styling
    - No functional regressions — all interactions work as before
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify visual polish</name>
  <files>n/a</files>
  <action>
    Complete visual polish pass on all UI components using frontend-design plugin. Fixed faint hint text, unreadable crown labels, and overall design quality.
  </action>
  <verify>
    1. Open http://mini:5173/
    2. Landing page: Check overall visual quality of title, description, file picker
    3. Load a photo (preferably one with a light/white background)
    4. Check: Crown "top of skull, not hair" hint is readable against the photo
    5. Check: "Drag to pan..." hint text below crop frame is clearly visible (not faint)
    6. Check: Export buttons, spec panel, header all look cohesive and polished
    7. Check: Overall impression — does it look production-grade?
  </verify>
  <done>User approves visual quality as production-grade, or provides specific feedback for iteration.</done>
</task>

</tasks>

<verification>
- `bun vite build` succeeds without errors
- Visual inspection confirms all cosmetic improvements
- No functional regressions in crop, zoom, pan, export
</verification>

<success_criteria>
- All cosmetic UAT gaps resolved (hint text visibility, crown label readability)
- User approves overall visual quality as production-grade
- frontend-design plugin was used for the design pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-cropping-tool-digital-export/01-11-SUMMARY.md`
</output>
