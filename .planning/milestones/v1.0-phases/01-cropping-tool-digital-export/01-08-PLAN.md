---
phase: 01-cropping-tool-digital-export
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/panZoom.ts
  - src/lib/actions/panZoom.test.ts
  - src/lib/components/CropEditor.svelte
autonomous: true
gap_closure: true
requirements: [CROP-05, CROP-06]

must_haves:
  truths:
    - "Normal scroll (no shift) zooms at coarse increments (~5% per step)"
    - "Shift+scroll zooms at fine increments (~1% per step)"
    - "Normal arrow keys pan at coarse steps (10px)"
    - "Shift+arrow keys pan at fine steps (2px)"
    - "Keyboard +/- zoom uses the same factors as mouse scroll zoom"
  artifacts:
    - path: "src/lib/actions/panZoom.ts"
      provides: "wheelToZoomFactor with inverted shift semantics and unified factors"
      contains: "shiftKey ? 1.01 : 1.05"
    - path: "src/lib/actions/panZoom.test.ts"
      provides: "Tests reflecting inverted modifier semantics"
    - path: "src/lib/components/CropEditor.svelte"
      provides: "Keyboard handlers with inverted shift and unified zoom factors"
      contains: "shiftKey ? 2 : 10"
  key_links:
    - from: "src/lib/actions/panZoom.ts"
      to: "src/lib/components/CropEditor.svelte"
      via: "Both use the same zoom factors (1.05 coarse / 1.01 fine)"
      pattern: "1\\.05|1\\.01"
---

<objective>
Invert the shift modifier for all inputs (scroll zoom, keyboard pan, keyboard zoom) and unify zoom increment factors between mouse and keyboard.

Purpose: UAT reported shift+scroll "broken" and zoom increments inconsistent between input methods. After this fix: no-shift = coarse (default, most common), shift = fine (precision). Both keyboard and mouse use identical zoom factors.

Output: Consistent zoom/pan behavior across all input methods with inverted modifier semantics.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current code that needs modification -->

From src/lib/actions/panZoom.ts (line 21):
```typescript
// CURRENT (wrong): shift=coarse, no-shift=fine
const step = shiftKey ? 1.05 : 1.01;
```

From src/lib/components/CropEditor.svelte (lines 57-58):
```typescript
// CURRENT (wrong): shift=coarse, no-shift=fine
const panStep = e.shiftKey ? 10 : 2;
const zoomFactor = e.shiftKey ? 1.1 : 1.03;
// Also wrong: keyboard zoom factors (1.03/1.1) differ from mouse (1.01/1.05)
```

From src/lib/components/CropEditor.svelte (line 116):
```typescript
aria-label="Crop editor: arrow keys to pan, +/- to zoom, Shift for coarse"
```

From src/lib/components/CropEditor.svelte (line 168):
```html
Drag to pan · Scroll to zoom · Arrows to pan · +/- to zoom · Shift for coarse
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert shift modifier and unify zoom factors</name>
  <files>src/lib/actions/panZoom.ts, src/lib/actions/panZoom.test.ts, src/lib/components/CropEditor.svelte</files>
  <behavior>
    - wheelToZoomFactor(100, false) returns 1.05 (coarse, no shift)
    - wheelToZoomFactor(100, true) returns 1.01 (fine, with shift)
    - wheelToZoomFactor(-100, false) returns 1/1.05 (coarse zoom in)
    - wheelToZoomFactor(-100, true) returns 1/1.01 (fine zoom in)
    - deltaX fallback with shift: wheelToZoomFactor(0, true, 100) returns 1.01
    - Coarse factor (no shift) is farther from 1 than fine factor (shift)
  </behavior>
  <action>
    Three locations need ternary branch swaps plus factor unification:

    1. **src/lib/actions/panZoom.ts line 21**: Change `shiftKey ? 1.05 : 1.01` to `shiftKey ? 1.01 : 1.05`. This inverts shift semantics so no-shift=coarse (1.05), shift=fine (1.01).

    2. **src/lib/components/CropEditor.svelte lines 57-58**:
       - panStep: Change `e.shiftKey ? 10 : 2` to `e.shiftKey ? 2 : 10`
       - zoomFactor: Change `e.shiftKey ? 1.1 : 1.03` to `e.shiftKey ? 1.01 : 1.05` — this both inverts the modifier AND unifies the factors with the mouse scroll values (1.05 coarse / 1.01 fine).

    3. **src/lib/actions/panZoom.test.ts**: Update all shift-related test expectations:
       - "returns coarse factor (1.05) with shiftKey=true" becomes "returns fine factor (1.01) with shiftKey=true"
       - "coarse factor is farther from 1 than fine factor" — swap: fine (shift) is closer to 1 than coarse (no shift)
       - deltaX fallback test: shiftKey=true now returns 1.01 not 1.05
       - The test for shiftKey=false should now expect 1.05 (was 1.01)

    4. **src/lib/components/CropEditor.svelte line 116**: Change aria-label to "Crop editor: arrow keys to pan, +/- to zoom, Shift for precision"

    5. **src/lib/components/CropEditor.svelte line 168**: Change hint text to "Drag to pan · Scroll to zoom · Arrows to pan · +/- to zoom · Shift for precision"

    Update the JSDoc comment on wheelToZoomFactor to say "Normal scroll = 5% per step (coarse). Shift+scroll = 1% per step (fine/precision)."
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run src/lib/actions/panZoom.test.ts</automated>
  </verify>
  <done>
    - No-shift scroll zooms at 5% per step (factor 1.05)
    - Shift+scroll zooms at 1% per step (factor 1.01)
    - No-shift arrow keys pan at 10px steps
    - Shift+arrow keys pan at 2px steps
    - Keyboard +/- uses same factors as mouse scroll (1.05/1.01)
    - Hint text and aria-label say "Shift for precision"
    - All panZoom tests pass with inverted expectations
  </done>
</task>

</tasks>

<verification>
- `bun vitest run src/lib/actions/panZoom.test.ts` — all tests pass with inverted shift semantics
- Grep confirms no remaining `shiftKey ? 1.05` in panZoom.ts (should be `shiftKey ? 1.01 : 1.05`)
- Grep confirms no remaining `1.1` or `1.03` zoom factors in CropEditor.svelte
- Hint text contains "precision" not "coarse"
</verification>

<success_criteria>
- All zoom/pan inputs use unified factors: 1.05 (coarse/default) and 1.01 (fine/shift)
- Shift modifier inverted in all 3 locations
- Tests updated and passing
- Hint text updated
</success_criteria>

<output>
After completion, create `.planning/phases/01-cropping-tool-digital-export/01-08-SUMMARY.md`
</output>
