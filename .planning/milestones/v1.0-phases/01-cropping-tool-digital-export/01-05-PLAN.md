---
phase: 01-cropping-tool-digital-export
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/panZoom.ts
  - src/lib/actions/panZoom.test.ts
autonomous: true
gap_closure: true
requirements:
  - CROP-03

must_haves:
  truths:
    - "Normal scroll zooms at 1% per step (factor 1.01)"
    - "Shift+scroll zooms at 5% per step (factor 1.05) for coarse control"
  artifacts:
    - path: "src/lib/actions/panZoom.ts"
      provides: "wheelToZoomFactor with shiftKey parameter"
      contains: "shiftKey"
    - path: "src/lib/actions/panZoom.test.ts"
      provides: "Tests for both zoom granularities"
      contains: "shiftKey"
  key_links:
    - from: "src/lib/actions/panZoom.ts"
      to: "CropEditor.svelte"
      via: "use:panZoom action wiring — onWheel reads shiftKey"
      pattern: "e\\.shiftKey"
---

<objective>
Fix zoom granularity: normal scroll = 1% per step (fine), shift+scroll = 5% per step (coarse). Shift means coarse everywhere, consistently.

Purpose: UAT gap #1 — 3% per step too coarse. CONTEXT.md locked decision: normal=1%, shift=5%, shift=coarse.
Output: Updated panZoom.ts with shift-aware wheel handler, updated tests.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cropping-tool-digital-export/01-UAT.md

<interfaces>
<!-- Current signatures in panZoom.ts that will change -->

From src/lib/actions/panZoom.ts:
```typescript
export function wheelToZoomFactor(deltaY: number): number {
  return deltaY > 0 ? 1.03 : 1 / 1.03;
}
// Called from onWheel handler inside panZoom action:
function onWheel(e: WheelEvent) {
  e.preventDefault();
  const factor = wheelToZoomFactor(e.deltaY);
  callbacks.onZoom(factor, e.offsetX, e.offsetY);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shift-aware zoom granularity to panZoom</name>
  <files>src/lib/actions/panZoom.ts, src/lib/actions/panZoom.test.ts</files>
  <behavior>
    - wheelToZoomFactor(deltaY, false) returns 1.01 or 1/1.01 (fine, normal scroll)
    - wheelToZoomFactor(deltaY, true) returns 1.05 or 1/1.05 (coarse, shift+scroll)
    - Existing tests still pass with shiftKey=false (backward compatible)
    - New tests verify shiftKey=true produces coarser factor (1.05)
    - onWheel handler inside panZoom action passes e.shiftKey to wheelToZoomFactor
  </behavior>
  <action>
1. Update `wheelToZoomFactor` signature to accept a second parameter `shiftKey: boolean = false`. When `shiftKey` is false (normal), use factor 1.01. When `shiftKey` is true (coarse), use factor 1.05. Per CONTEXT.md: "Shift = coarse everywhere."
2. Update the `onWheel` handler inside the `panZoom` function to pass `e.shiftKey` as the second argument to `wheelToZoomFactor`.
3. Update existing tests: the test "returns a consistent factor regardless of deltaY magnitude" should use factor 1.01 (was 1.03). Add explicit `false` to make intent clear.
4. Add new tests:
   - `wheelToZoomFactor with shiftKey=true returns coarse factor (1.05)` — verify the factor is 1.05 (farther from 1 than normal).
   - `wheelToZoomFactor with shiftKey=true returns inverse for opposite directions` — same symmetry check as existing test.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun run test -- --run src/lib/actions/panZoom.test.ts</automated>
  </verify>
  <done>wheelToZoomFactor accepts shiftKey param. Normal scroll = 1.01 (1%), shift+scroll = 1.05 (5%). Shift = coarse. All tests pass.</done>
</task>

</tasks>

<verification>
- `bun run test -- --run` — full test suite passes
- wheelToZoomFactor(100, false) === 1.01
- wheelToZoomFactor(100, true) === 1.05
</verification>

<success_criteria>
Normal scroll provides fine zoom (1% steps), shift+scroll provides coarse zoom (5% steps). Shift = coarse consistently. Tests cover both granularities.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cropping-tool-digital-export/01-05-SUMMARY.md`
</output>
