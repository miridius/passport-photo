---
phase: 02-print-layout
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/lib/components/PrintPreview.svelte
  - src/lib/components/ExportButtons.svelte
  - src/routes/+page.svelte
  - e2e/passphoto.test.ts
autonomous: true
requirements:
  - EXPP-01
  - EXPP-02
  - EXPP-03

must_haves:
  truths:
    - "User sees a 'Download Print Sheet' button alongside existing export buttons"
    - "Clicking the button shows a modal preview of the tiled sheet before downloading"
    - "User can confirm download or cancel from the preview modal"
    - "Downloaded file is a JPEG with 4 passport photos tiled on a 9x13cm sheet"
  artifacts:
    - path: "src/lib/components/PrintPreview.svelte"
      provides: "Preview modal using native dialog element"
      min_lines: 40
    - path: "src/lib/components/ExportButtons.svelte"
      provides: "Third export button triggering print sheet generation"
    - path: "e2e/passphoto.test.ts"
      provides: "E2E tests for print sheet button and preview modal"
  key_links:
    - from: "src/lib/components/ExportButtons.svelte"
      to: "src/lib/canvas/printSheet.ts"
      via: "import renderPrintSheet, calculateSheetLayout"
      pattern: "renderPrintSheet"
    - from: "src/lib/components/ExportButtons.svelte"
      to: "src/lib/components/PrintPreview.svelte"
      via: "passes blob to preview modal"
      pattern: "PrintPreview"
    - from: "src/lib/components/PrintPreview.svelte"
      to: "src/lib/canvas/export.ts"
      via: "triggerDownload for confirmed download"
      pattern: "triggerDownload"
---

<objective>
Wire the print sheet generation into the UI: add a "Download Print Sheet" button, build a preview modal with native `<dialog>`, and add E2E tests covering the full flow.

Purpose: This connects the layout math (Plan 02) and the resized crop frame (Plan 01) into a user-facing feature that completes all three EXPP requirements.
Output: PrintPreview.svelte, updated ExportButtons.svelte, E2E tests.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-print-layout/02-CONTEXT.md
@.planning/phases/02-print-layout/02-01-SUMMARY.md
@.planning/phases/02-print-layout/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: printSheet.ts exports -->
<!-- These will exist after Plan 02 completes -->
```typescript
export interface SheetLayout {
  sheetWidthPx: number;
  sheetHeightPx: number;
  tiles: TilePosition[];
  pxPerMm: number;
}
export interface TilePosition {
  tileX: number; tileY: number;
  photoX: number; photoY: number;
  photoWidthPx: number; photoHeightPx: number;
}
export function calculateSheetLayout(cropWidthPx: number): SheetLayout;
export async function renderPrintSheet(img: HTMLImageElement, crop: CropState, layout: SheetLayout): Promise<Blob>;
```

<!-- From existing code -->
From src/lib/canvas/export.ts:
```typescript
export function triggerDownload(blob: Blob, filename: string): void;
```

From src/lib/components/ExportButtons.svelte:
```typescript
// Pattern: $state for loading flags, $derived for hasImage, async handler with try/finally
let exportingPrint = $state(false);
const hasImage = $derived(imageState.element !== null);
```

From src/lib/state/crop.svelte.ts:
```typescript
export let cropState = $state<CropState>(...);
export let imageState = $state<ImageState>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PrintPreview modal and integrate export button</name>
  <files>src/lib/components/PrintPreview.svelte, src/lib/components/ExportButtons.svelte, src/routes/+page.svelte</files>
  <action>
    1. **PrintPreview.svelte**: Create a modal component using native `<dialog>` element.
       - Props/exports: a `show(blob: Blob)` function that creates an object URL from the blob and calls `dialogEl.showModal()`.
       - Internal state: `previewUrl` (string, object URL), `dialogEl` (HTMLDialogElement ref).
       - On close (`onclose` handler): revoke the object URL to prevent memory leaks, clear previewUrl.
       - Backdrop click to close: check `event.target === dialogEl` in the dialog's click handler.
       - Two buttons: "Download" (calls `triggerDownload` with the blob, then closes) and "Cancel" (closes dialog).
       - Store the blob in a local variable so Download can use it after the URL is created.
       - CSS: `::backdrop` with semi-transparent black. Image displayed with `max-width: 90vw; max-height: 80vh; object-fit: contain`. Centered layout. Actions bar below image. Style buttons to match existing export-btn pattern (Download in blue, Cancel in gray).
       - Use Svelte 5 runes ($state). Export the `show` function using `export function` syntax.

    2. **ExportButtons.svelte**: Add a third button "Download Print Sheet" after the existing Digital button.
       - New state: `let exportingSheet = $state(false)`.
       - New handler `downloadPrintSheet()`:
         a. Guard: `if (!imageState.element) return;`
         b. Set exportingSheet = true, clear exportError.
         c. Calculate cropWidthPx = `Math.round(cropState.zoomFraction * imageState.naturalWidth)`.
         d. Call `calculateSheetLayout(cropWidthPx)` to get layout.
         e. Call `await renderPrintSheet(imageState.element, cropState, layout)` to get blob.
         f. Call `previewRef.show(blob)` to open preview modal.
         g. Set exportingSheet = false in finally block.
       - Import `calculateSheetLayout` and `renderPrintSheet` from `$lib/canvas/printSheet`.
       - Import PrintPreview component: `let previewRef: PrintPreview` with `bind:this`.
       - Button styled with a new color (e.g., purple/violet `#7c3aed`) to differentiate from print (blue) and digital (green). Same sizing and structure as existing buttons.
       - Button text: `exportingSheet ? 'Generating...' : 'Download Print Sheet'` (no dynamic dimensions needed).

    3. **+page.svelte**: No changes needed -- ExportButtons.svelte is already rendered in the spec-column. The PrintPreview modal can live inside ExportButtons.svelte since it's triggered from there.

    4. **Actually**: Place `<PrintPreview bind:this={previewRef} />` inside ExportButtons.svelte at the end of the template. The `<dialog>` element renders in the top layer when `showModal()` is called, so DOM position doesn't matter for visual stacking.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run && bun playwright test</automated>
  </verify>
  <done>Print sheet button visible alongside existing buttons. Clicking it generates a sheet preview in a modal. User can download or cancel. Existing export buttons unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: E2E tests for print sheet workflow</name>
  <files>e2e/passphoto.test.ts</files>
  <action>
    Add E2E tests to the existing test file:

    1. **Print sheet button visible**: After loading an image, verify a button with text matching /Print Sheet/i is visible and enabled.

    2. **Print sheet preview modal opens**: Click the print sheet button, wait for a `<dialog[open]>` element to appear. Verify the dialog contains an `<img>` element (the preview). Verify "Download" and "Cancel" buttons are present inside the dialog.

    3. **Preview modal dismiss on cancel**: Open the preview, click Cancel, verify the dialog is no longer open.

    4. **Preview modal dismiss on backdrop click**: Open the preview, click the dialog backdrop (click at dialog element coordinates but outside the inner content). Verify dialog closes. (This test may be tricky -- if hard to get right, skip and note in summary.)

    5. **Download from preview**: Open the preview, set up a download listener (`page.waitForEvent('download')`), click Download. Verify a download was triggered with a `.jpg` filename.

    Note: renderPrintSheet requires Canvas which works in Playwright's Chromium. These tests should NOT skip on chromium.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun playwright test</automated>
  </verify>
  <done>E2E tests cover: button visibility, modal open/close, download trigger. All pass in Chromium.</done>
</task>

</tasks>

<verification>
- `bun vitest run` -- all unit tests pass (no regressions)
- `bun playwright test` -- all E2E tests pass including new print sheet flow tests
- Manual: load a photo, click "Download Print Sheet", see preview with 4 tiled photos and cut marks, click Download, verify JPEG downloads
</verification>

<success_criteria>
1. "Download Print Sheet" button appears alongside existing Print and Digital buttons
2. Clicking button generates sheet and shows preview modal
3. Preview shows tiled photos with cut marks on white background
4. Download button triggers JPEG download, Cancel dismisses modal
5. No memory leaks (object URL revoked on close)
6. All unit and E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-print-layout/02-03-SUMMARY.md`
</output>
