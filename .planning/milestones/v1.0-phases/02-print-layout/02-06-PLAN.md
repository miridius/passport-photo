---
phase: 02-print-layout
plan: 06
type: execute
wave: 2
depends_on: [02-04]
files_modified:
  - e2e/passphoto.test.ts
  - src/lib/state/spec.ts
autonomous: true
gap_closure: true
requirements: [EXPP-01, EXPP-02]

must_haves:
  truths:
    - "Backdrop click on preview modal dismisses the dialog (verified by E2E test)"
    - "E2E aspect ratio assertion matches 35:45 (not 36:46)"
    - "E2E dimension bracket test uses /45 fractions (not /46)"
    - "PRINT_SHEET.cutMark.lengthMm is 0.5 (matches actual cut mark implementation)"
  artifacts:
    - path: "e2e/passphoto.test.ts"
      provides: "Backdrop dismiss E2E test, corrected aspect ratio and bracket assertions"
    - path: "src/lib/state/spec.ts"
      provides: "PRINT_SHEET.cutMark.lengthMm updated to 0.5"
  key_links:
    - from: "e2e/passphoto.test.ts"
      to: "src/lib/components/PrintPreview.svelte"
      via: "dialog backdrop click behavior"
      pattern: "dialog\\[open\\]"
---

<objective>
Add E2E test for backdrop dismiss and fix E2E assertions for 35:45 crop frame.

Purpose: Backdrop dismiss is deterministic browser behavior that should be E2E-tested, not manual UAT. Also, after Plan 04 reverts the crop frame to 35:45, E2E tests asserting 36:46 ratios and /46 fractions need updating. And PRINT_SHEET.cutMark.lengthMm needs to match the 0.5mm implementation from Plan 05.

Output: Complete E2E coverage for print sheet workflow, correct assertions post-revert.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-print-layout/02-UAT.md
@.planning/phases/02-print-layout/02-04-PLAN.md

<interfaces>
<!-- PrintPreview.svelte backdrop dismiss mechanism -->
```typescript
// In PrintPreview.svelte:
function handleBackdropClick(e: MouseEvent) {
  if (e.target === dialogEl) dialogEl.close();
}
// <dialog ... onclick={handleBackdropClick}>
```

<!-- After Plan 04, the crop frame uses 35:45 -->
<!-- Guide fractions after Plan 04 revert: -->
```typescript
crownGuide.topFrac = 4 / 45;      // ~0.0889
crownGuide.bottomFrac = 7 / 45;   // ~0.1556
chinGuide.topFrac = 39 / 45;      // ~0.8667
chinGuide.bottomFrac = 40 / 45;   // ~0.8889
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backdrop dismiss E2E test and fix assertions</name>
  <files>e2e/passphoto.test.ts</files>
  <action>
1. Add a new E2E test "print sheet preview closes on backdrop click" after the existing "print sheet preview closes on cancel" test:
   - Load photo, click Print Sheet button, wait for dialog[open] to be visible.
   - Click the dialog backdrop: use `dialog.click({ position: { x: 5, y: 5 } })` to click the top-left corner of the dialog element (which is the backdrop area, outside the dialog content). The `<dialog>` element itself covers the viewport when opened with showModal(), and clicking the element (not its children) triggers the backdrop dismiss.
   - Alternative approach: get the dialog's bounding box and click at coordinates just inside the dialog element but outside the visible content area. Since the dialog has padding/margin, clicking at `{ x: 1, y: 1 }` relative to the dialog should hit the backdrop.
   - Assert the dialog is no longer visible: `await expect(dialog).not.toBeVisible()`.

2. Fix aspect ratio assertion in "loads a photo and displays crop frame with 36:46 aspect ratio" test:
   - Change test name to "loads a photo and displays crop frame with 35:45 aspect ratio"
   - Change `expect(ratio).toBeCloseTo(36 / 46, 1)` to `expect(ratio).toBeCloseTo(35 / 45, 1)`

3. Fix dimension bracket test "dimension brackets align with guide bands":
   - Change `(7.5 / 46)` to `(7 / 45)` for crown bottom fraction
   - Change `(39.5 / 46)` to `(39 / 45)` for chin top fraction

4. Keep all other existing tests unchanged — they test behaviors that don't depend on the aspect ratio.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run</automated>
  </verify>
  <done>E2E test file has backdrop dismiss test. Aspect ratio assertions use 35/45. Dimension bracket fractions use /45. Unit tests still pass (E2E tests verified separately by user).</done>
</task>

<task type="auto">
  <name>Task 2: Sync PRINT_SHEET.cutMark.lengthMm with implementation</name>
  <files>src/lib/state/spec.ts</files>
  <action>
In src/lib/state/spec.ts, update PRINT_SHEET.cutMark.lengthMm from 2 to 0.5 to match the actual CUT_MARK_LENGTH_MM in printSheet.ts (changed by Plan 05).

This is a documentation-level constant — printSheet.ts uses its own inline constant, not this one. But keeping them in sync prevents confusion.

Also update the spec.test.ts assertion for cutMark.lengthMm from 2 to 0.5 if it exists. Check first:
- If spec.test.ts has `expect(PRINT_SHEET.cutMark.lengthMm).toBe(2)`, change to `.toBe(0.5)`.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run src/lib/state/spec.test.ts</automated>
  </verify>
  <done>PRINT_SHEET.cutMark.lengthMm = 0.5 in spec.ts. Corresponding test updated. Spec tests pass.</done>
</task>

</tasks>

<verification>
1. `bun vitest run` — all unit tests pass
2. `grep "36 / 46" e2e/passphoto.test.ts` — no results (all 36:46 references replaced with 35:45)
3. `grep "backdrop" e2e/passphoto.test.ts` — finds the new backdrop dismiss test
4. `grep "lengthMm" src/lib/state/spec.ts` — shows 0.5
</verification>

<success_criteria>
- E2E test for backdrop dismiss exists and is syntactically correct
- All E2E aspect ratio assertions use 35/45
- All E2E dimension bracket fractions use /45 denominator
- PRINT_SHEET.cutMark.lengthMm = 0.5
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-print-layout/02-06-SUMMARY.md`
</output>
