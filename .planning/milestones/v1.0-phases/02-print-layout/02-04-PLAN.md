---
phase: 02-print-layout
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/state/spec.ts
  - src/lib/state/spec.test.ts
  - src/lib/components/CropEditor.svelte
autonomous: true
gap_closure: true
requirements: [EXPP-01]

must_haves:
  truths:
    - "Crop frame displays at 35:45 aspect ratio (not 36:46)"
    - "Rulers span 0-35mm horizontal and 0-45mm vertical with no buffer offset"
    - "Guide bands calibrated to 45mm frame height (fractions use /45 denominator)"
    - "Single-photo exports produce 35:45 photos with no buffer baked in"
    - "PRINT_SHEET constants remain unchanged for print sheet layout"
  artifacts:
    - path: "src/lib/state/spec.ts"
      provides: "AU_PASSPORT_SPEC without frame* fields, guides at /45, PRINT_SHEET intact"
      contains: "topFrac: 4 / 45"
    - path: "src/lib/state/spec.test.ts"
      provides: "Tests for reverted guide fractions and removed frame fields"
      contains: "4 / 45"
    - path: "src/lib/components/CropEditor.svelte"
      provides: "35:45 aspect ratio, rulers without buffer offset"
      contains: "aspect-ratio: 35 / 45"
  key_links:
    - from: "src/lib/components/CropEditor.svelte"
      to: "src/lib/state/spec.ts"
      via: "imports AU_PASSPORT_SPEC for photoWidthMm/photoHeightMm only"
      pattern: "AU_PASSPORT_SPEC\\.photo(Width|Height)Mm"
    - from: "src/lib/components/GuideOverlay.svelte"
      to: "src/lib/state/spec.ts"
      via: "imports crownGuide/chinGuide with /45 fractions"
      pattern: "crownGuide|chinGuide"
---

<objective>
Revert crop frame from 36:46 to 35:45 aspect ratio, removing the cutting buffer from the crop UI.

Purpose: The 0.5mm cutting buffer belongs only in the print sheet tile layout (printSheet.ts), not in the crop frame. Phase 02 Plan 01 incorrectly applied it to the crop frame, causing all exports to include the buffer in photo content and miscalibrating guide bands.

Output: Crop frame at 35:45, guides at /45 fractions, rulers without offset, frame* fields removed from AU_PASSPORT_SPEC.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-print-layout/02-01-SUMMARY.md
@.planning/phases/02-print-layout/02-UAT.md

<interfaces>
<!-- Original state before phase 02 changed it (git show 9937301^:src/lib/state/spec.ts) -->

Guide fractions BEFORE phase 02 (revert to these):
```typescript
export const crownGuide: GuidePosition = {
	topFrac: 4 / 45,    // 4mm from top
	bottomFrac: 7 / 45, // 7mm from top — 3mm band
};

export const chinGuide: GuidePosition = {
	topFrac: 39 / 45,    // 39mm from top
	bottomFrac: 40 / 45, // 40mm from top — 1mm bar
};
```

CropEditor.svelte BEFORE phase 02:
- `aspect-ratio: 35 / 45` (not 36/46)
- Ruler ticks: `style="top: {(mm / H) * 100}%;"` (H = 45, no buffer)
- Horizontal ticks: `style="left: {(mm / W) * 100}%;"` (W = 35, no buffer)
- Keyboard zoom: `frameHeight = frameWidth * (45 / 35)`
- No FH, FW, or buf variables

AU_PASSPORT_SPEC BEFORE phase 02 had NO:
- `frameWidthMm`, `frameHeightMm`, `frameAspectRatio`, `bufferMm`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert spec.ts guides and remove frame fields</name>
  <files>src/lib/state/spec.ts, src/lib/state/spec.test.ts</files>
  <action>
In src/lib/state/spec.ts:
1. Remove these fields from AU_PASSPORT_SPEC: `frameWidthMm`, `frameHeightMm`, `frameAspectRatio`, `bufferMm` (lines 14-18 in current file).
2. Revert crownGuide to use /45 denominators: `topFrac: 4 / 45`, `bottomFrac: 7 / 45`.
3. Revert chinGuide to use /45 denominators: `topFrac: 39 / 45`, `bottomFrac: 40 / 45`.
4. Update the comment block above the guides to reference "45mm frame" instead of "46mm frame". The original comments explained: face 32-36mm centered in 45mm, crown at 4mm/7mm from top, chin at 39mm/40mm from top. Close edges 7 to 39 = 32mm, far edges 4 to 40 = 36mm.
5. PRINT_SHEET constant stays UNCHANGED — it correctly has its own tile/photo/inset dimensions.

In src/lib/state/spec.test.ts:
1. Remove the test "has correct frame aspect ratio (36:46 with cutting buffer)" (entire it block).
2. Remove the test "has frame dimensions with 0.5mm buffer" (entire it block).
3. Update "guide band positions" describe block: change title from "(36:46 frame)" to "(35:45 frame)".
4. Update crown band test: assert `4 / 45` and `7 / 45` instead of `4.5 / 46` and `7.5 / 46`.
5. Update chin bar test: assert `39 / 45` and `40 / 45` instead of `39.5 / 46` and `40.5 / 46`.
6. Update close edges test: use `frameHeight = 45` instead of 46.
7. Update far edges test: use `frameHeight = 45` instead of 46.
8. Update face centered test: use `frameHeight = 45` denominator (or remove if it only tested 46mm centering).
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run src/lib/state/spec.test.ts</automated>
  </verify>
  <done>AU_PASSPORT_SPEC has no frame* or buffer* fields. Guide fractions use /45 denominators. All spec tests pass. PRINT_SHEET unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Revert CropEditor to 35:45 aspect ratio and simple rulers</name>
  <files>src/lib/components/CropEditor.svelte</files>
  <action>
In src/lib/components/CropEditor.svelte:

Script section:
1. Remove variables FH, FW, buf (currently lines 12-14: `const FH = AU_PASSPORT_SPEC.frameHeightMm;`, etc.)
2. Change guide position calculations to use H (45) instead of FH (46):
   - `const crownTopMm = crownGuide.topFrac * H;` (H is already defined as photoHeightMm = 45)
   - Same for crownBottomMm, chinTopMm, chinBottomMm
3. Change keyboard zoom frame height from `frameWidth * (FH / FW)` to `frameWidth * (H / W)` (line ~68, equivalent to `frameWidth * (45 / 35)`)

Template section:
1. Vertical ruler ticks: change `style="top: {((buf + mm) / FH) * 100}%;"` back to `style="top: {(mm / H) * 100}%;"` (line 89)
2. Horizontal ruler ticks: change `style="left: {((buf + mm) / FW) * 100}%;"` back to `style="left: {(mm / W) * 100}%;"` (line 147)

Style section:
1. Change `.crop-frame` `aspect-ratio: 36 / 46;` to `aspect-ratio: 35 / 45;` (line 184)
2. Change `.crop-frame` `width: calc(80vh * 36 / 46);` to `width: calc(80vh * 35 / 45);` (line 186)

Verify the import line still only imports what it needs from spec.ts. Since frameWidthMm/frameHeightMm/bufferMm are removed from the spec, the import should just use `AU_PASSPORT_SPEC` for photoWidthMm/photoHeightMm (which it already does via H and W).
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run</automated>
  </verify>
  <done>Crop frame is 35:45. Rulers use simple mm/H and mm/W fractions. No references to FH, FW, buf, frameWidthMm, frameHeightMm, or bufferMm in CropEditor.svelte. All unit tests pass.</done>
</task>

</tasks>

<verification>
1. `bun vitest run` — all unit tests pass (spec, printSheet, crop state, export)
2. `grep -r "frameWidthMm\|frameHeightMm\|frameAspectRatio\|bufferMm" src/lib/state/spec.ts` — no results
3. `grep "aspect-ratio" src/lib/components/CropEditor.svelte` — shows `35 / 45`
4. `grep "/ FH\|/ FW\|buf +" src/lib/components/CropEditor.svelte` — no results
</verification>

<success_criteria>
- Crop frame displays at 35:45 aspect ratio
- Rulers show 0-35mm / 0-45mm aligned to frame edges (no buffer offset)
- Guide bands at 4/45, 7/45, 39/45, 40/45 (32mm and 36mm distances preserved)
- Single-photo exports produce correct 35:45 crops (no buffer in content)
- PRINT_SHEET constants untouched
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-print-layout/02-04-SUMMARY.md`
</output>
