---
phase: 02-print-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/state/spec.ts
  - src/lib/state/spec.test.ts
  - src/lib/components/CropEditor.svelte
  - src/lib/components/GuideOverlay.svelte
  - src/lib/canvas/export.ts
  - src/lib/canvas/export.test.ts
  - e2e/passphoto.test.ts
autonomous: true
requirements:
  - EXPP-01

must_haves:
  truths:
    - "Crop frame displays at 36:46 aspect ratio (not 35:45)"
    - "Rulers still show 0-35mm (vertical) and 0-45mm (horizontal), offset 0.5mm from frame edges"
    - "Guide bands remain calibrated to 35x45mm inner zone within the 36x46mm frame"
    - "Single-photo exports still produce correct crops (unchanged behavior)"
  artifacts:
    - path: "src/lib/state/spec.ts"
      provides: "PRINT_SHEET constants and updated aspectRatio for 36:46 frame"
      contains: "PRINT_SHEET"
    - path: "src/lib/state/spec.test.ts"
      provides: "Tests for new constants and recalculated guide fractions"
    - path: "src/lib/components/CropEditor.svelte"
      provides: "36:46 aspect-ratio crop frame with offset rulers"
    - path: "src/lib/components/GuideOverlay.svelte"
      provides: "Guide bands recalculated for 36:46 frame"
  key_links:
    - from: "src/lib/state/spec.ts"
      to: "src/lib/components/CropEditor.svelte"
      via: "aspectRatio import"
      pattern: "AU_PASSPORT_SPEC\\.aspectRatio"
    - from: "src/lib/state/spec.ts"
      to: "src/lib/components/GuideOverlay.svelte"
      via: "guide fraction imports"
      pattern: "crownGuide|chinGuide"
---

<objective>
Resize the crop frame from 35:45 to 36:46 aspect ratio to provide a 0.5mm cutting buffer on all sides, and add print sheet constants to spec.ts. Rulers stay at 0-35mm / 0-45mm but are offset 0.5mm from frame edges. Guide bands recalibrate to the 36:46 frame while remaining calibrated to the 35x45mm inner zone.

Purpose: The 36x46mm tile size is a prerequisite for accurate print sheets -- it provides the cutting buffer that DM Sofortfoto needs. This plan also adds the sheet layout constants needed by Plan 02.
Output: Updated crop frame, guide bands, rulers, and new PRINT_SHEET constants in spec.ts.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-print-layout/02-CONTEXT.md
@.planning/phases/02-print-layout/02-RESEARCH.md

<interfaces>
<!-- Existing types and exports the executor needs -->

From src/lib/types.ts:
```typescript
export interface CropState {
  offsetX: number;
  offsetY: number;
  zoomFraction: number;
}
export interface ExportTarget {
  widthPx: number;
  heightPx: number;
  dpi: number;
  format: 'png' | 'jpeg';
  quality?: number;
}
export interface GuidePosition {
  topFrac: number;
  bottomFrac: number;
}
```

From src/lib/state/spec.ts:
```typescript
export const AU_PASSPORT_SPEC = {
  photoWidthMm: 35,
  photoHeightMm: 45,
  aspectRatio: 35 / 45,
  // ... other fields
};
export const crownGuide: GuidePosition = { topFrac: 4/45, bottomFrac: 7/45 };
export const chinGuide: GuidePosition = { topFrac: 39/45, bottomFrac: 40/45 };
```

From src/lib/components/CropEditor.svelte (crop frame aspect ratio):
```css
.crop-frame {
  aspect-ratio: 35 / 45;
  width: calc(80vh * 35 / 45);
}
```

From src/lib/components/GuideOverlay.svelte (guide band positioning):
```svelte
style="top: {crownGuide.topFrac * 100}%; height: {(crownGuide.bottomFrac - crownGuide.topFrac) * 100}%;"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PRINT_SHEET constants and recalibrate frame/guide dimensions</name>
  <files>src/lib/state/spec.ts, src/lib/state/spec.test.ts</files>
  <behavior>
    - PRINT_SHEET.sheetWidthMm = 90, sheetHeightMm = 130
    - PRINT_SHEET.tileWidthMm = 36, tileHeightMm = 46
    - PRINT_SHEET.photoWidthMm = 35, photoHeightMm = 45
    - PRINT_SHEET.gapMm = 5, insetMm = 0.5
    - PRINT_SHEET.cols = 2, rows = 2
    - PRINT_SHEET.cutMark = { lengthMm: 2, color: '#333333', lineWidthAtDpi: 0.6 }
    - AU_PASSPORT_SPEC.frameAspectRatio = 36/46 (new field, existing aspectRatio stays 35/45 for crop math)
    - AU_PASSPORT_SPEC.frameWidthMm = 36, frameHeightMm = 46 (new fields)
    - AU_PASSPORT_SPEC.bufferMm = 0.5 (new field)
    - crownGuide fractions recalculated for 46mm frame: topFrac = (0.5 + 4) / 46, bottomFrac = (0.5 + 7) / 46
    - chinGuide fractions recalculated for 46mm frame: topFrac = (0.5 + 39) / 46, bottomFrac = (0.5 + 40) / 46
    - Guide close edges still 32mm apart in physical space
    - Guide far edges still 36mm apart in physical space
    - Existing tests updated for new guide fractions (now dividing by 46, not 45)
    - All margins computable: marginX = (90 - 2*36 - 5) / 2 = 3.5mm, marginY = (130 - 2*46 - 5) / 2 = 16.5mm
  </behavior>
  <action>
    1. Add `PRINT_SHEET` constant object to spec.ts with all sheet/tile/cut-mark dimensions per CONTEXT.md locked decisions.
    2. Add `frameAspectRatio: 36/46`, `frameWidthMm: 36`, `frameHeightMm: 46`, `bufferMm: 0.5` to AU_PASSPORT_SPEC. Keep existing `aspectRatio: 35/45` for crop math (used by calculateSourceRect and initial zoom calculation).
    3. Recalculate crownGuide and chinGuide fractions. The guides are positioned within the 45mm inner zone, but fractions are now relative to the 46mm frame. Formula: `(bufferMm + originalMm) / 46`. Crown: topFrac = 4.5/46, bottomFrac = 7.5/46. Chin: topFrac = 39.5/46, bottomFrac = 40.5/46.
    4. Update spec.test.ts: adjust guide fraction assertions to use /46 denominator. Add tests for PRINT_SHEET constants. Verify close edges are still 32mm and far edges are still 36mm (in physical mm, accounting for the 0.5mm buffer offset). Verify margin calculations.

    IMPORTANT: The crownGuide/chinGuide fractions change meaning -- they were fractions of 45mm frame, now fractions of 46mm frame. This is intentional. GuideOverlay.svelte uses `topFrac * 100%` which will now be relative to the 46mm frame height, placing bands correctly within the 35x45mm inner zone.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run src/lib/state/spec.test.ts</automated>
  </verify>
  <done>PRINT_SHEET constants exist with correct values. Guide fractions recalculated for 46mm frame. Close/far edge distances still 32mm/36mm in physical space. All spec tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update crop frame, rulers, and exports for 36:46 aspect ratio</name>
  <files>src/lib/components/CropEditor.svelte, src/lib/components/GuideOverlay.svelte, src/lib/canvas/export.ts, src/lib/canvas/export.test.ts, e2e/passphoto.test.ts</files>
  <action>
    1. **CropEditor.svelte**: Change `.crop-frame` CSS from `aspect-ratio: 35/45` to `aspect-ratio: 36/46` and `width: calc(80vh * 36/46)`. Import `frameAspectRatio`, `frameWidthMm`, `frameHeightMm`, `bufferMm` from spec. Ruler tick computation: vertical ruler still 0-45mm with ticks at `((bufferMm + mm) / frameHeightMm) * 100%`. Horizontal ruler still 0-35mm with ticks at `((bufferMm + mm) / frameWidthMm) * 100%`. The keyboard zoom center uses `frameWidth * (45/35)` -- update to `frameWidth / (36/46)` or derive from frameAspectRatio.

    2. **GuideOverlay.svelte**: No changes needed -- it already reads crownGuide/chinGuide fractions from spec.ts, and those fractions were recalculated in Task 1 to account for the 46mm frame.

    3. **CropEditor.svelte ruler math**: The `H` and `W` constants used for tick generation stay at 45 and 35 (ruler labels unchanged). But tick positioning shifts: `top: ((0.5 + mm) / 46) * 100%` for vertical, `left: ((0.5 + mm) / 36) * 100%` for horizontal. This offsets rulers 0.5mm from the frame edge.

    4. **export.ts**: The `calculateSourceRect` function uses `targetHeight / targetWidth` ratio. For single-photo exports, this is still 45/35 (the photo content ratio). No change needed -- the export target dimensions are set by ExportButtons.svelte which already uses `45/35` ratio. The print sheet (Plan 02) will pass 35x45 photo dimensions, not 36x46 tile dimensions.

    5. **crop.svelte.ts**: The `loadImage` function uses `AU_PASSPORT_SPEC.aspectRatio` for initial zoom. This is still 35/45 (crop content ratio). But the frame is now 36:46. The initial zoom should use `frameAspectRatio` (36/46) since the frame shape determines how much of the image fills the viewport. Update: import `frameAspectRatio` and use it in the aspect comparison. Actually, the existing `aspectRatio` (35/45 = 0.778) and `frameAspectRatio` (36/46 = 0.783) are very close. The difference is negligible for initial zoom. Leave as-is to avoid touching crop.svelte.ts (HMR-sensitive).

    6. **E2E tests**: Update the aspect ratio test from `35/45` to `36/46`. The guide band and ruler alignment tests may need minor tolerance adjustments since the guides are now offset within the frame.

    7. **export.test.ts**: Verify calculateSourceRect still works correctly -- no changes to the function, but add a comment noting that export targets use 35x45 photo dimensions, not 36x46 frame dimensions.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run && bun playwright test</automated>
  </verify>
  <done>Crop frame renders at 36:46 aspect ratio. Rulers show 0-35mm / 0-45mm offset 0.5mm from edges. Guide bands correctly positioned within 35x45mm inner zone. All existing export functionality unchanged. Unit and E2E tests pass.</done>
</task>

</tasks>

<verification>
- `bun vitest run` -- all unit tests pass including updated spec and export tests
- `bun playwright test` -- E2E tests pass with 36:46 frame ratio
- Visual: crop frame is very slightly wider relative to height (36:46 = 0.783 vs 35:45 = 0.778)
- Rulers read 0-35mm and 0-45mm, not 0-36mm and 0-46mm
</verification>

<success_criteria>
1. Crop frame aspect ratio is 36:46
2. Rulers labeled 0-35mm / 0-45mm, positioned with 0.5mm inset from frame edges
3. Guide bands remain at correct physical positions (32mm/36mm face height preserved)
4. PRINT_SHEET constants available for Plan 02
5. All unit and E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-print-layout/02-01-SUMMARY.md`
</output>
