---
phase: 02-print-layout
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/canvas/printSheet.ts
  - src/lib/canvas/printSheet.test.ts
  - src/lib/state/spec.ts
  - src/lib/state/spec.test.ts
autonomous: true
requirements: [EXPP-02]
gap_closure: true

must_haves:
  truths:
    - "Cut mark arms are 2mm long filled rectangles extending outward from tile edge into gap/margin"
    - "Cut mark arms are 0.5mm wide (matching buffer zone width)"
    - "Cut marks sit entirely outside the tile boundary"
    - "Horizontal arms are Y-aligned with the buffer zone strip; vertical arms are X-aligned"
  artifacts:
    - path: "src/lib/canvas/printSheet.ts"
      provides: "drawCutMarks using fillRect for 2mm x 0.5mm arms outside tile"
      contains: "CUT_MARK_LENGTH_MM = 2"
    - path: "src/lib/canvas/printSheet.test.ts"
      provides: "Tests validating filled-rect geometry, arm dimensions, and tile-external positioning"
    - path: "src/lib/state/spec.ts"
      provides: "PRINT_SHEET.cutMark.lengthMm synced to 2"
  key_links:
    - from: "src/lib/canvas/printSheet.ts"
      to: "canvas fillRect API"
      via: "ctx.fillRect for each arm"
      pattern: "ctx\\.fillRect"
---

<objective>
Fix cut mark geometry: arms are 2mm-long x 0.5mm-wide filled rectangles extending outward from tile corners into the gap/margin area.

Purpose: UAT blocker — user reported cut marks are "tiny and in the wrong spot." Previous change confused arm length (2mm) with arm width (0.5mm), and drew stroked lines at photo boundary instead of filled rects outside tile boundary.
Output: Correct cut mark rendering with passing unit tests.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-print-layout/02-UAT.md

@src/lib/canvas/printSheet.ts
@src/lib/canvas/printSheet.test.ts
@src/lib/state/spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite cut mark geometry — filled rects outside tile boundary</name>
  <files>src/lib/canvas/printSheet.ts, src/lib/canvas/printSheet.test.ts, src/lib/state/spec.ts, src/lib/state/spec.test.ts</files>
  <behavior>
    - Test: CUT_MARK_LENGTH_MM equals 2
    - Test: Each cut mark corner produces exactly 2 fillRect calls (horizontal arm + vertical arm)
    - Test: Top-left horizontal arm is a 2mm x 0.5mm rect extending LEFT from tile left edge (x = tileX - 2mm*pxPerMm, y = tileY, width = 2mm*pxPerMm, height = 0.5mm*pxPerMm)
    - Test: Top-left vertical arm is a 0.5mm x 2mm rect extending UP from tile top edge (x = tileX, y = tileY - 2mm*pxPerMm, width = 0.5mm*pxPerMm, height = 2mm*pxPerMm)
    - Test: Bottom-right horizontal arm extends RIGHT from tile right edge (x = tileX + tileWidthPx, y = tileY + tileHeightPx - 0.5mm*pxPerMm)
    - Test: Bottom-right vertical arm extends DOWN from tile bottom edge (y = tileY + tileHeightPx)
    - Test: No fillRect call overlaps the tile area (all rects fully outside tileX..tileX+tileWidthPx, tileY..tileY+tileHeightPx)
    - Test: PRINT_SHEET.cutMark.lengthMm equals 2 in spec.ts
  </behavior>
  <action>
    1. In `printSheet.ts`, change `CUT_MARK_LENGTH_MM` from 0.5 back to 2.

    2. Rewrite `drawCutMarks` to use `ctx.fillRect` instead of stroked L-paths:
       - Set `ctx.fillStyle = CUT_MARK_COLOR` (no stroke needed)
       - Remove `CUT_MARK_LINE_WIDTH_AT_300DPI` — not needed for filled rects
       - Calculate tile bounds: `tileRight = tile.tileX + TILE_WIDTH_MM * pxPerMm`, `tileBottom = tile.tileY + TILE_HEIGHT_MM * pxPerMm`
       - Arm dimensions: `armLength = CUT_MARK_LENGTH_MM * pxPerMm` (2mm), `armWidth = INSET_MM * pxPerMm` (0.5mm)
       - For each of the 4 corners, draw 2 filled rects OUTSIDE the tile:
         - **Top-left**: horizontal arm at (tileX - armLength, tileY, armLength, armWidth), vertical arm at (tileX, tileY - armLength, armWidth, armLength)
         - **Top-right**: horizontal arm at (tileRight, tileY, armLength, armWidth), vertical arm at (tileRight - armWidth, tileY - armLength, armWidth, armLength)
         - **Bottom-left**: horizontal arm at (tileX - armLength, tileBottom - armWidth, armLength, armWidth), vertical arm at (tileX, tileBottom, armWidth, armLength)
         - **Bottom-right**: horizontal arm at (tileRight, tileBottom - armWidth, armLength, armWidth), vertical arm at (tileRight - armWidth, tileBottom, armWidth, armLength)

    3. Remove the `drawLMark` helper function (no longer needed).

    4. Add `tileWidthPx` and `tileHeightPx` to `TilePosition` interface (or compute inline from constants and pxPerMm). If adding to interface, update `calculateSheetLayout` to populate them.

    5. In `spec.ts`, update `PRINT_SHEET.cutMark.lengthMm` from 0.5 to 2.

    6. In `spec.test.ts`, update the assertion for `cutMark.lengthMm` to expect 2.

    7. Rewrite `printSheet.test.ts` cut mark tests:
       - Mock ctx with a `fillRect` spy (record all fillRect calls as {x, y, w, h})
       - Test 4 corners x 2 arms = 8 fillRect calls per tile
       - Test exact coordinates for top-left and bottom-right corners
       - Test arm dimensions: each arm is either (armLength x armWidth) or (armWidth x armLength)
       - Test all rects are fully outside tile bounding box
       - Remove old stroked-line tests (moveTo/lineTo assertions)
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run src/lib/canvas/printSheet.test.ts src/lib/state/spec.test.ts</automated>
  </verify>
  <done>
    - CUT_MARK_LENGTH_MM is 2, INSET_MM is 0.5
    - drawCutMarks uses fillRect (not stroke/lineTo)
    - All 8 arms per tile are outside the tile boundary
    - Arms are 2mm long x 0.5mm wide
    - PRINT_SHEET.cutMark.lengthMm is 2
    - All unit tests pass
  </done>
</task>

</tasks>

<verification>
- `bun vitest run` — all unit tests pass (printSheet + spec)
- grep for `ctx.stroke` in printSheet.ts returns zero matches (no stroked lines)
- grep for `CUT_MARK_LENGTH_MM = 2` confirms correct value
</verification>

<success_criteria>
Cut marks are 2mm-long filled rectangles extending outward from each tile corner into gap/margin area, 0.5mm wide (matching buffer zone). No overlap with tile content. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-print-layout/02-07-SUMMARY.md`
</output>
