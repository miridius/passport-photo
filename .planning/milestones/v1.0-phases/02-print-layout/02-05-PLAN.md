---
phase: 02-print-layout
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/canvas/printSheet.ts
  - src/lib/canvas/printSheet.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [EXPP-02, EXPP-03]

must_haves:
  truths:
    - "Cut marks delineate the 0.5mm buffer zone between photo edge and tile edge"
    - "Cut marks are 0.5mm segments, not 2mm L-shapes"
    - "Cut marks appear at all four corners of each tile"
    - "REQUIREMENTS.md EXPP-01 says 9x13cm (matching user-confirmed decision)"
    - "REQUIREMENTS.md EXPP-03 says native resolution JPEG (no 300 DPI mandate)"
    - "changeDpiBlob behavior is verified or removed if unnecessary"
  artifacts:
    - path: "src/lib/canvas/printSheet.ts"
      provides: "Redesigned drawCutMarks with 0.5mm buffer-zone marks"
      exports: ["calculateSheetLayout", "drawCutMarks", "renderPrintSheet"]
    - path: "src/lib/canvas/printSheet.test.ts"
      provides: "Updated tests for new cut mark geometry"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated EXPP-01 and EXPP-03 text"
  key_links:
    - from: "src/lib/canvas/printSheet.ts"
      to: "src/lib/canvas/export.ts"
      via: "import { calculateSourceRect }"
      pattern: "calculateSourceRect"
---

<objective>
Redesign cut marks to delineate the buffer zone and clarify DPI requirements.

Purpose: Cut marks currently draw 2mm L-shapes at the photo boundary. User wants marks that delineate the 0.5mm buffer strip between the 35x45mm photo edge and the 36x46mm tile edge. Additionally, "300 DPI" was auto-derived, not user-specified — CONTEXT.md says "native source resolution".

Output: Cut marks that visually indicate the buffer zone, updated REQUIREMENTS.md.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-print-layout/02-02-SUMMARY.md
@.planning/phases/02-print-layout/02-UAT.md
@.planning/phases/02-print-layout/02-CONTEXT.md

<interfaces>
<!-- Current printSheet.ts exports (unchanged by this plan except drawCutMarks internals) -->

```typescript
export interface TilePosition {
  tileX: number;   // Top-left of 36x46mm tile
  tileY: number;
  photoX: number;  // Top-left of 35x45mm photo (tileX + 0.5mm inset)
  photoY: number;
  photoWidthPx: number;
  photoHeightPx: number;
}

export interface SheetLayout {
  sheetWidthPx: number;
  sheetHeightPx: number;
  tiles: TilePosition[];
  pxPerMm: number;
}

export function calculateSheetLayout(cropWidthPx: number): SheetLayout;
export function drawCutMarks(ctx: CanvasRenderingContext2D, tile: TilePosition, pxPerMm: number): void;
export async function renderPrintSheet(img: HTMLImageElement, crop: CropState, layout: SheetLayout): Promise<Blob>;
```

Key geometry:
- Tile: 36x46mm (tileX, tileY)
- Photo: 35x45mm (photoX, photoY) = 0.5mm inset from tile
- Buffer zone: the 0.5mm strip on each side between photo edge and tile edge
- Cut marks should indicate where this buffer zone is at each corner
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign cut marks to delineate 0.5mm buffer zone</name>
  <files>src/lib/canvas/printSheet.ts, src/lib/canvas/printSheet.test.ts</files>
  <behavior>
    - Cut marks at each corner consist of two short segments (0.5mm long each), positioned to mark the buffer zone boundaries
    - At top-left corner: one horizontal segment from (tileX, photoY) to (photoX, photoY) marking the top-left buffer width, and one vertical segment from (photoX, tileY) to (photoX, photoY) marking the top-left buffer height
    - This creates an L-shape where each arm is exactly 0.5mm (the buffer width), sitting in the buffer zone
    - Arms extend from the photo corner toward the tile corner (inward into the buffer, not outward into the gap)
    - All four corners follow the same pattern, mirrored appropriately
    - No mark point is strictly inside the photo area (on boundary is OK)
    - Mark color remains #333333, line width remains 0.6px at 300 DPI
  </behavior>
  <action>
In src/lib/canvas/printSheet.ts:
1. Change `CUT_MARK_LENGTH_MM = 2` to `CUT_MARK_LENGTH_MM = 0.5` (matching the buffer/inset width).
2. Redesign `drawCutMarks` so the L-mark arms point inward toward the tile corner rather than outward into the gap. Each arm is 0.5mm = INSET_MM in length. The corner vertex of each L is at the photo boundary corner (photoX, photoY for top-left). The arms extend into the buffer zone:
   - Top-left: horizontal arm from (photoX - insetPx, photoY) to (photoX, photoY), vertical arm from (photoX, photoY - insetPx) to (photoX, photoY). This means drawLMark(ctx, photoX, photoY, -insetPx, -insetPx) with the EXISTING drawLMark function — it draws from (x+dx, y) to (x, y) to (x, y+dy).
   - Top-right: drawLMark(ctx, right, top, +insetPx, -insetPx)
   - Bottom-left: drawLMark(ctx, left, bottom, -insetPx, +insetPx)
   - Bottom-right: drawLMark(ctx, right, bottom, +insetPx, +insetPx)

   Wait — the current drawLMark signature draws arms AWAY from the corner. For buffer zone marks, the arms need to go FROM the tile edge TO the photo edge. So the vertex is still at the photo corner, and the arms go toward the tile corner. The current code already uses `drawLMark(ctx, left, top, -armLen, -armLen)` which means horizontal arm goes left (toward tile edge) and vertical arm goes up (toward tile edge). So the direction is actually CORRECT — just the length changes from 2mm to 0.5mm. The only issue is the arms currently extend PAST the tile edge into the gap (2mm >> 0.5mm). With 0.5mm arms, they'll land exactly at the tile edge.

   Simplification: just change `CUT_MARK_LENGTH_MM` from 2 to 0.5 and keep the same drawLMark calls. The arms will be 0.5mm long, going from photo corner to tile corner, perfectly delineating the buffer zone.

3. Also update the constant name for clarity. Optionally rename from CUT_MARK_LENGTH_MM to CUT_MARK_ARM_MM or keep as is.

In src/lib/canvas/printSheet.test.ts:
1. Update the "top-left mark arms point outward" test: armLen should now be 0.5 * pxPerMm = 5px (not 2 * 10 = 20px). Update expected coordinates.
2. Update the "bottom-right mark arms point outward" test similarly.
3. Update test descriptions from "point outward" to "delineate buffer zone" or similar.
4. Update the "cut marks never overlap photo area" test — the marks should still pass (all points on boundary or in buffer zone).
5. Add a new test: "cut mark arms are exactly inset length (0.5mm)" — verify armLen = INSET_MM * pxPerMm.

Also update PRINT_SHEET.cutMark.lengthMm from 2 to 0.5 in spec.ts... Actually, PRINT_SHEET is in spec.ts which is modified by Plan 04. But Plan 04 doesn't touch PRINT_SHEET. And printSheet.ts uses its own CUT_MARK_LENGTH_MM constant, not PRINT_SHEET.cutMark.lengthMm. So update PRINT_SHEET.cutMark.lengthMm from 2 to 0.5 in spec.ts as well.

Wait — spec.ts is in Plan 04's files_modified. To avoid file conflicts: include the PRINT_SHEET.cutMark.lengthMm update in THIS plan and note the shared file. Actually, let me reconsider: Plan 04 only touches AU_PASSPORT_SPEC and guides in spec.ts. PRINT_SHEET is separate. But to avoid parallel write conflicts, add spec.ts to this plan's files_modified and handle just the cutMark.lengthMm field change.

Actually, since both plans are wave 1 (parallel), having both modify spec.ts creates a conflict. Let's keep it simple: printSheet.ts has its own CUT_MARK_LENGTH_MM constant (line 18). Just change that. The PRINT_SHEET.cutMark.lengthMm in spec.ts is documentation-only (printSheet.ts doesn't import it). We can update spec.ts PRINT_SHEET.cutMark.lengthMm in Plan 06 (wave 2) or leave it as a documentation discrepancy to fix later. Better: Plan 04 already modifies spec.ts and spec.test.ts — add a note in Plan 04's action to also update PRINT_SHEET.cutMark.lengthMm from 2 to 0.5.

No — Plan 04 is already written. Keep it simple: in THIS plan, only modify printSheet.ts and printSheet.test.ts. The PRINT_SHEET constant in spec.ts is a documentation-level value. Update it in Plan 06 as a trivial addition, or note it for the executor.

Final approach: only change CUT_MARK_LENGTH_MM in printSheet.ts from 2 to 0.5. Don't touch spec.ts from this plan.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && bun vitest run src/lib/canvas/printSheet.test.ts</automated>
  </verify>
  <done>Cut marks are 0.5mm L-shapes delineating the buffer zone at each photo corner. Arms go from photo edge to tile edge. All printSheet tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md for confirmed decisions</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In .planning/REQUIREMENTS.md:

1. Update EXPP-01 text: change "10x15cm sheet" to "9x13cm sheet" to match user-confirmed decision in CONTEXT.md. The full line should read:
   `- [x] **EXPP-01**: User can generate a print layout that tiles the cropped photo onto a 9x13cm sheet (standard DM Sofortfoto format)`

2. Update EXPP-03 text: remove "at 300 DPI" mandate. The user said "native source resolution" — 300 DPI was auto-derived. Change to:
   `- [x] **EXPP-03**: Print layout is downloadable as a JPEG image with DPI metadata for correct physical sizing`

3. Investigate changeDpiBlob: The function IS called in renderPrintSheet (line 172 of printSheet.ts). It writes JFIF DPI metadata into the JPEG blob. This metadata tells printers/viewers the intended physical size. Even without a 300 DPI mandate, having DPI metadata is beneficial for DM Sofortfoto printing — it ensures the sheet prints at the right physical dimensions. Keep the changeDpiBlob(blob, 300) call. The 300 value is appropriate because the layout math uses pxPerMm derived from the source crop resolution, and 300 DPI (11.81 px/mm) is the standard print resolution reference.

Note: No code changes needed for DPI — just the requirements text update.
  </action>
  <verify>
    <automated>cd /home/me/repos/passphoto && grep "EXPP-01\|EXPP-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>EXPP-01 says "9x13cm" (not 10x15cm). EXPP-03 says "DPI metadata for correct physical sizing" (not "at 300 DPI"). changeDpiBlob call retained as beneficial.</done>
</task>

</tasks>

<verification>
1. `bun vitest run src/lib/canvas/printSheet.test.ts` — all cut mark tests pass with 0.5mm arm length
2. `bun vitest run` — full unit test suite green
3. `grep "CUT_MARK_LENGTH_MM" src/lib/canvas/printSheet.ts` — shows 0.5
4. `grep "EXPP-01" .planning/REQUIREMENTS.md` — shows "9x13cm"
5. `grep "EXPP-03" .planning/REQUIREMENTS.md` — no "at 300 DPI" mandate
</verification>

<success_criteria>
- Cut marks are 0.5mm arm length, delineating the buffer zone between photo and tile edges
- All 4 corners of all 4 tiles get cut marks
- No cut mark overlaps the photo content area
- REQUIREMENTS.md EXPP-01 references 9x13cm (matching CONTEXT.md)
- REQUIREMENTS.md EXPP-03 reflects native resolution with DPI metadata (no hard 300 DPI requirement)
- changeDpiBlob retained for practical print benefit
</success_criteria>

<output>
After completion, create `.planning/phases/02-print-layout/02-05-SUMMARY.md`
</output>
